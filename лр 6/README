Задание 1:
Создал класс UserAccount с атрибутами username, email, password и инициализировал их (__init__).
Приватный атртбут через _ .
Реализовал метод set_password, который изменяет пароль. (self._password = new_password)
Реализвал метод check_password, который проверяет соответствует ли введенный пароль текущему паролю.
Создал объект класса.

Задание 2:
Создал класс Car, наследующий класс Vehicle через super.
super — это встроенная функция, которая позволяет вызывать методы родительского класса из дочернего класса.
Также добавил атрибут fuel_type (через self, как и ранее)
