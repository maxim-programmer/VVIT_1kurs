1 задание:
Запросил у пользователя ввод числа и преобразовал введенные данные в целое число. (int(input()))
Использовал цикл for, чтобы вывести числа от 1 до введенного значения включительно. (for i in range())
Цикл for в Python — это конструкция для повторного выполнения блока кода для каждого элемента последовательности (например, списка, строки, диапазона чисел).

2 задание:
Запросил у пользователя два числа и преобразовал введенные значения в целые числа. (int(input()))
Сравнил числа с помощью конструкции if-elif-else.
Вывел большее число.
